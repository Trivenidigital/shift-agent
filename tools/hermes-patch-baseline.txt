# Hermes upstream baseline — drift detection for our patches.
#
# This file pins the Hermes version + commit hash + bridge.js sha256 that our
# patches were authored/verified against. tools/check-shift-agent-patch.sh
# fail-closes the deploy if the live VPS Hermes drifts from any of these.
#
# Update this file when:
#   1. You upgrade Hermes (new commit hash, possibly new version, possibly new
#      bridge.js content). Run check-shift-agent-patch.sh with the override
#      env vars to ship the upgrade, then commit the updated baseline.
#   2. You re-run tools/patch-bridge-filter.py and the bridge.js content
#      changes deterministically. Capture the new sha256 + commit it.
#
# Fields are shell-sourceable (KEY=VALUE, no spaces).
#
# 2026-05-23 re-baseline: fleet Hermes upgrade moved Main to v0.14.0
# commit 1e71b718. Verified check-shift-agent-patch.sh with an explicit
# one-run override against the live Main gateway/bridge after the upgrade;
# captured the post-upgrade bridge.js sha256 below.
#
# 2026-07-12 front-brain Phase-1: added the egress screen patch to
# gateway/platforms/whatsapp.py — BOTH text-egress paths: send() (marker
# shift-agent-front-brain-send, before format/relay) and edit_message() (marker
# shift-agent-front-brain-edit, before the /edit relay; streamed drafts +
# finalized answer via stream_consumer.py). Both wrap `content` with
# safe_io.front_brain_screen_gateway_send. Authored against Hermes commit
# 1e71b7180e5b4e84905b9a3086cf9cecca139562 (== the pin below — UNCHANGED) and
# unpatched whatsapp.py sha256
# d59426d5cdb18a09bed91b07521cd6d7e190a59a9222e2ed235d6a1b23513663. whatsapp.py
# is NOT content-sha-pinned here (same convention as the sender-id whatsapp.py
# patch — protected by the commit pin + marker + anchor-proximity checks in
# check-shift-agent-patch.sh), so no value below changes.
#
# 2026-08-07 re-baseline: Hermes 0.14.0 -> 0.19.1. The fleet upgrade to
# commit cc4cab2f (tag v2026.7.30) landed 2026-08-01 but this file was never
# regenerated, so the pin attested 0.14.0 while the runtime ran 0.19.1 and the
# deploy gate could only pass under HERMES_PIN_OVERRIDE.
#
# ARCHITECTURE CHANGE recorded with this re-baseline: the Python-side patches
# (gateway/run.py, gateway/platforms/whatsapp.py) were SUPERSEDED by the
# shift-agent-policy Hermes plugin, which keeps the Hermes checkout stock. On
# 0.19.1 the WhatsApp platform relocated to plugins/platforms/whatsapp/adapter.py
# and gateway/platforms/whatsapp.py no longer exists. Only the bridge.js JS-side
# patches remain in-tree, so BRIDGE_POST_PATCH_SHA256 is now the only content pin.
#
# BRIDGE_POST_PATCH_SHA256 below is REPRODUCIBLE, not a snapshot of the box:
#   pristine 0.19.1 bridge.js  9e1c4745da7d385a56fe3e48ff510e94f577ccd4cd01daa66c02d69267226185
#     + tools/hermes-patch-port-v0191/patch1_port_v0191.py
#     + tools/hermes-patch-port-v0191/patch2_failclosed.py
#   = f8bdb2ab... , byte-identical to the running bridge.
# Caveat recorded honestly: both scripts hard-code ROOT=/usr/local/lib/hermes-agent,
# so the reproduction used temporary copies differing by exactly that ONE line
# (diff-verified). Same algorithm, root path redirected -- NOT "ran as-is".
HERMES_COMMIT=cc4cab2f592e60a197e796506de9168f74baf3ea
HERMES_VERSION=0.19.1
BRIDGE_POST_PATCH_SHA256=f8bdb2abc2a2a5bc8f80b9eb6373fa67dc78a23793db92fd7e5552d11724bb0d
