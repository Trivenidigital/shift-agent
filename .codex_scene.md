Review branch `feat/flyer-skill-driven-scene` vs origin/main. Read `git diff origin/main...HEAD` and
the plan `tasks/flyer-skill-driven-integrated-renderer-plan-2026-06-07.md`.

CONTEXT: occasion quality fix. The integrated poster model renders graduation as a family dinner
because its scene/theme is picked by hardcoded Python keyword sets. The flyer_generation Hermes SKILL
already infers occasion visual_direction (verified live: graduation -> caps/diplomas/balloons). This
slice threads the SKILL's ADVISORY visual_direction into the integrated renderer's scene block.

OPERATOR'S 5 BINDING CORRECTIONS — verify each is honored:
1. DECOUPLED from FLYER_CREATIVE_DIRECTOR_ENABLED. The new advise_scene_direction()
   (flyer_context_builder.py) must NOT be build_flyer_brief and must NOT be gated by the CD flag; the
   new gate is FLYER_SKILL_DRIVEN_SCENE + allowlist (bare_render._skill_driven_scene_armed). Confirm
   build_flyer_brief semantics + the CD path are untouched.
2. NO fail-closed for scene guidance. advise_scene_direction must return None (never raise, never
   FAILCLOSED) on ANY problem (skill body unreadable, gateway disabled/missing-key/error/timeout,
   unparseable, missing/empty visual_direction). render_grounded must fall back to today's Python
   scene on None. Confirm there is NO new path where a vague request fails because of this.
3. visual_direction ONLY (not background_brief). Confirm advise reads only visual_direction and the
   scene block is composed from theme_family/palette/motifs/visual_subjects; background_brief unused.
4. Allowlist + scoped. Confirm the gate requires flag==1 AND allowlisted sender; off/not-allowlisted
   never calls the skill.
5. Truth-safety: the skill supplies ONLY theme/visual taste; the exact facts must still be injected by
   _poster_copy_block (Python), NOT sourced from the skill. Confirm _scene_block_from_visual_direction
   carries no business names/prices/dates and _image_prompt still calls _poster_copy_block.

ALSO CHALLENGE:
- The scene_direction threading (render_concept_previews -> _render_model -> _openrouter_image_bytes ->
  _image_message_content -> _image_prompt): any call site that now passes scene_direction to a function
  that doesn't accept it, or any default that breaks the flag-off path? (Flag off must be byte-identical
  to today.)
- Does _sanitize_visual_context get applied to the skill's strings (so a malicious/garbled skill value
  can't inject prices/phones into the prompt)?
- Is there any way an empty/partial VisualDirection produces a worse prompt than today's Python scene
  (it should fall back instead)?

This deploys flag-OFF (3 flat files: flyer_render.py, flyer_bare_render.py, flyer_context_builder.py),
then enabled for an allowlisted test sender. CD stays off. Output CLEAN or findings (BLOCKER/MAJOR/MINOR).
