Re-confirm (round 2) of `feat/flyer-skill-driven-scene` vs origin/main. Round-1 had two MAJORs, both
fixed in HEAD:
1. _scene_block_from_visual_direction no longer injects the business name (dropped the business param;
   block is pure visual taste — render.py). Confirm no fact (business/price/date) is in the scene block
   and _image_prompt still injects facts via _poster_copy_block.
2. advise_scene_direction now requires a substantive direction: `if not vd.theme_family.strip() or not
   (vd.visual_subjects or vd.motifs): return None` (flyer_context_builder.py). Confirm a thin/partial
   skill response falls back to the Python scene rather than producing a weaker prompt.

Review `git diff origin/main...HEAD`. Confirm both fixes are correct and complete, and the 5 operator
corrections still hold (decoupled from CD flag; never fail-closed; visual_direction only; allowlist;
facts stay in _poster_copy_block). Flag-off path byte-identical to today. CLEAN or findings.
