# Lessons

## 2026-05-22 - Flyer Hermes intent layer

- When working in isolated worktrees, verify the plan/design file landed in the intended worktree before dispatching reviewers. A plan written to the dirty root checkout can make reviewers correctly report "file missing" even though the main session sees it.
- Flyer route observability must capture the terminal customer-visible route, not the first audit row. `flyer_active_project_bypassed` can be an intermediate event before `flyer_primary_project_created`; self-eval should compare against the final route sequence.
- Do not let a shadow classifier become a third router. For PR-1, Hermes intent evidence should be schema/validator/route-trace infrastructure only; live active routing needs separate promotion criteria and per-family shadow data.

## 2026-05-21 - Flyer post-preview visible text revisions

- Post-preview Flyer corrections can reference visible generated text that is not a structured project fact. Exact remove/delete requests such as `Time: 16:00 is duplicated. remove this` should enter the revision/regeneration path instead of asking for "exact item or text" clarification just because the text was not in field state.
- When hardening revision parsing, pin both the pure workflow parser and the `update-flyer-project` state-file response; the customer-facing cf-router branch depends on `revision_requires_clarification`, not only on intent classification.

## 2026-05-13 — Hermes-first and deployed-state first

- Before live smoke interpretation or follow-on work, verify the active deployed Hermes mode/config and current code path. Do not infer from an older handoff.
- For this project, treat `cf-router`/Hermes substrate as the first explanation for inbound behavior. Only describe a dispatcher chain as healthy after audit evidence shows the deployed path actually reached it.
- Enforce Hermes-first before any custom code, plan, review, or bug-fix proposal: check repo primitives, deployed VPS Hermes skills, deployed VPS Hermes plugins, and the Hermes ecosystem before scoping net-new work.
- When saying "Hermes capabilities", include what is actually installed on the VPS under `/root/.hermes/skills` and `/root/.hermes/plugins`, not just source files in this checkout.
- Related Hermes ecosystem checks to remember: Self-Evolution Kit at `https://github.com/NousResearch/hermes-agent-self-evolution` and Awesome Hermes Agent at `https://github.com/0xNyk/awesome-hermes-agent`.
- For "no key / no token" Hermes work, perform current market research across installed Hermes skills/plugins, official Hermes skills, Awesome Hermes, vendor MCP servers, iPaaS MCP options, and manual-export fallbacks before labeling anything custom-only.
- Do not treat `employee` sender identity as "never a customer." Employees can submit legitimate catering inquiries for their own/family/friend events. Owner identity remains control-plane; employee identity can still enter customer-side catering when intent is clear.
- For active catering leads, do not require a follow-up message to independently satisfy the new-inquiry classifier. Weak menu/proposal/food/event signals can be enough to route to the existing lead's follow-up branch; otherwise status/menu follow-ups fall into the generic LLM.
- For SSH on Windows, always use the two-step redirect/read pattern; never rely on inline SSH stdout.

## 2026-05-14 — Production pilot posture

- First production pilot bundle is **Shift Agent + Catering Agent + Daily Brief Agent**. Do not start by adding a greenfield third agent; Daily Brief is the owner control tower that makes Shift/Catering operable.
- Production readiness must be enforced by a deterministic gate, not by memory. Use `/usr/local/bin/pilot-readiness-check --text` before saying a customer VPS is ready for the first three-agent pilot.
- Current `main-vps` readiness after deploy `deploy-20260514-170739-f4ce14db`: gateway active, WhatsApp bridge connected, timers active, roster valid, catering menu valid with 78 available items. Blocking rows are only `customer.name` and `customer.location_id` placeholders.
- For catering menu updates, verified owner OR verified employee may submit a menu image/PDF source, but only the owner may apply the extracted menu with the confirmation code. Preserve that split in future dispatcher/SKILL changes.
- Self-learning/evolution rule for production: live agents may learn state and memory (menus, LIDs, customer notes, lead history, roster facts) but must not mutate code, SKILLs, prompts, or deploy config in prod. Skill/code evolution goes through traces/evals, tests, review, PR, and tarball deploy.
- For a 3-4 day customer ask, treat this as a production pilot with explicit smoke evidence, not broad GA. Use `docs/runbooks/production-pilot-shift-catering-daily-brief.md` as the acceptance script.
- Commit before deploy whenever possible. The 2026-05-14 pilot gate was deployed from an uncommitted working tree, so the deploy tag uses the previous HEAD hash; future production deploys should commit first for traceability unless the user explicitly asks for an emergency hot deploy.
- When editing runtime YAML, account for schema defaults that may not be present in the file. If appending a list item such as `daily_brief.sections`, first materialize the full intended default list plus the new item; otherwise a default-backed config can be narrowed accidentally.
- For pydantic-backed production CLIs on the VPS, run through `/usr/local/lib/hermes-agent/venv/bin/python` or an equivalent Hermes-venv shebang. System Python can lack pydantic and make direct operator/systemd execution diverge from smoke tests.

## 2026-05-15 - Flyer routing and cf-router precedence

- When adding a new WhatsApp agent, verify pre-gateway plugins before trusting the dispatcher matrix. `cf-router` can skip the LLM and dispatcher entirely, so new intent families need explicit bypass/priority guards there too.
- Food/event/festival words inside a flyer brief are not Catering intent when an explicit flyer/design keyword or active flyer project exists. Guard the deterministic Catering F7 branch before weak follow-up suppression, especially when the sender has an old active catering lead.
- Do not stop at "cf-router returns None" for live WhatsApp readiness. A new agent must also prove the next gateway/dispatcher validation boundary accepts the real inbound sender block and can invoke the target skill without fail-closed UX.
- When running production smoke probes as root against state files normally owned by `shift-agent`, immediately verify ownership/mode or chown the agent state directory. Root-created JSON state with `0600` can pass root probes and then fail real WhatsApp traffic with `PermissionError`.
- Flyer workflow smoke is not product-quality proof. A deterministic/Pillow renderer can validate state, delivery, and PDF packaging, but customer readiness requires a real image-generation path and a visual quality gate before calling the agent production-ready.
- Flyer Studio must optimize for one-shot quality and credit discipline. Do not default to three image concepts with expensive models; generate one best design, ask for `APPROVE` or changes, and export final formats from the selected image without spending image credits again.
- Long-running Flyer image generation must send an immediate WhatsApp acknowledgement before the model call. Customer-visible responsiveness is part of correctness; no silent multi-minute waits after pressing send.
- Flyer revisions are not notes-only. Any revision must invalidate the current selected design/final assets, apply high-confidence structured field edits (date/time/etc.), regenerate a design, and block `APPROVE` until the revised design is ready.
- Flyer menu/promo requests are not always one-time events. Breakfast menus, weekend specials, and recurring offers may omit a date legitimately; parse location/address/phone/menu details, allow recurring schedules without fake dates, and pass the full menu/pricing text into the image prompt.
- Active Flyer projects must not swallow explicit new flyer work. If a sender has an old awaiting-approval project, route "need/create flyer..." and media-backed menu/template edits as new projects, not as revisions to stale state.
- Uploaded flyer/template images need project-level reference assets when the sender is not onboarded yet. Customer brand storage alone is not enough, because unauthenticated/pending senders may not have an active customer profile for the renderer to read.
- Menu and price-list flyers can be complete without event date/time/venue. For product lists with prices and a contact number, generate the flyer instead of waiting for event fields that do not exist.
- Do not synthesize E.164 phone numbers from `@lid` values in Flyer routing. If `identify-sender` cannot resolve a phone/LID mapping, fail closed rather than creating account, quota, or project state under a fabricated identity.
- For money-adjacent onboarding, make payment references immutable history, not just "latest scalar on customer." A reused Stripe/Razorpay/manual reference must remain blocked even after plan changes.
- Account scripts that read Pydantic schemas must run through the Hermes venv in cf-router and smoke/deploy paths. Direct shebang execution can accidentally use system Python and fail only in production.
- Saved logo/template replacement is an account-admin action. Authorized flyer requesters can request flyers, but permanent brand-kit changes need the business WhatsApp/onboarding admin or explicit owner role.
- Flyer Studio marketing must be business-category neutral unless targeting a specific vertical campaign. User examples like item swaps or price edits describe a revision pattern, not restaurant-only copy. Lead with universal owner pain: lost time, slow approvals, repeated revisions, and getting finished marketing assets without design software.
- Keep Hermes as an internal platform name only. Customer-facing Flyer Studio marketing, WhatsApp outreach, flyers, onboarding language, and sales conversations must not mention Hermes.
- For Flyer Studio outbound campaigns, do not put the whole pitch in the WhatsApp text body. Send a strong visual flyer first; keep the message short with only the calls to action such as "Start Free Trial" and "Act Now! Save Time and Money" plus links.
- Flyer Studio campaign creative must be broadly useful across restaurants, salons, tutors, realtors, temples, service businesses, and local retailers. Avoid product-specific examples like Dosa/Idly/Poori/Parota unless the campaign is explicitly restaurant-specific.
- For Flyer Studio marketing flyers, keep the "100+ businesses" social-proof badge unless the user explicitly asks to remove it.
- For customer-facing Flyer Studio outreach, do not expose raw URLs when a true WhatsApp button/CTA/link mechanism is available. Preferred visible CTAs are "Start Free Trial" and "Act Now! Save Time and Money."
- Flyer Studio campaign CTA labels must not share the same backend action. "Start Free Trial" should prefill a ready-to-create trial opener, while "Act Now! Save Time and Money" should prefill onboarding/setup intent so routing and customer expectations stay distinct.
- Flyer Studio WhatsApp campaign buttons should be reply/action buttons, not URL CTA buttons, when the user expects one-tap intent inside the chat. URL buttons open browser/dialog UI; they do not send or type an inbound message to the agent.
- Campaign CTA replies must be handled before active-project/new-project flyer intake. Button-generated text such as "Start Free Trial" or "Help me create a beautiful flyer for my business" is onboarding/ready-prompt intent, not a flyer brief, even if it contains the word "flyer."
- Hermes may pass WhatsApp inbound text to hooks with a leading `[shift-agent-sender ...]` block. Exact-match routing rules must normalize to the visible message body before matching, or live traffic can bypass tests that used plain text.
- Campaign CTA retries can land while an onboarding session is waiting for a specific field. Treat campaign CTA text as a restart/start control before parsing field answers, or stale partial sessions can try to parse "Help me create..." as a phone number and fall through to the LLM.
- Once Flyer onboarding has started, arbitrary short replies such as `1` must route back into onboarding before the generic LLM. The onboarding script should return field-specific repair prompts for invalid values instead of raising and letting Hermes compose a generic apology.
- Active Flyer onboarding should outrank explicit new-flyer/project intent until the account is complete. Otherwise normal user replies containing words like "need flyer" can create orphan projects before the business profile and trial/account state exist.
- Flyer Studio plan copy should position the $199 Unlimited tier as a higher-touch service, not only higher quota. Mention designer-assisted manual edits for custom requests when showing the Unlimited plan.
- Flyer Studio onboarding must accept compound confirmation replies such as `CONFIRM. Create ...` by completing registration first and then routing the trailing flyer brief as a new project. Do not force the customer to repeat a request they already sent.
- Flyer Studio active intake projects must never allow short customer replies to fall through to the generic LLM. If the project cannot deterministically parse the reply, send a Flyer Studio project-scoped prompt instead.
- Flyer Studio must not save arbitrary customer media as a permanent logo/template just because the sender is registered. Permanent brand-kit capture requires an explicit logo/template/reference/brand cue or an active flyer project context.
- Never clear live Flyer customer state for a number the user considers already onboarded just to force a first-time test. If a clean test is needed, use a temp state file or a different test number; production state must preserve real onboarding history.
- Flyer Studio campaign CTAs must be idempotent by account state: new sender starts onboarding, in-progress sender resumes/restarts intentionally, payment-pending sender gets payment/status guidance, and trial/active sender gets a ready/account prompt without being asked for business details again.
- Flyer Studio stale onboarding sessions must not outrank a trial or active customer account. Any onboarding/session intercept should first resolve the sender to an existing trial/active customer and let explicit flyer requests route to project creation; otherwise old `confirming_summary` state can trap real customer work and loop into duplicate-phone errors.
- Flyer Studio duplicate-phone recovery must handle second-sender retries for the same business. If all duplicate business phones resolve to one existing account and the business name is a close match, connect the sender as an authorized requester, preserve pending brand assets, clear the onboarding session, and send the existing-account ready prompt instead of leaving the user stuck at `EDIT WHATSAPP`.
- Flyer Studio image quality failures can come from the app compositor even when the image model is high quality. Do not accept a raw transcript/debug overlay as "text safety"; menu flyers need structured item extraction, clean title/schedule copy, and a designed menu-card overlay that preserves the AI food art.
- Flyer Studio customer-quality baseline is full poster composition, not background art plus an overlay. Restaurant/menu flyers must preserve or recreate the reference hierarchy: brand masthead, large promo title, item/category cards, readable prices, food photos tied to each item/category, footer contact/actions, and visual QA against the user-provided baseline before sending to customers.
- Flyer Studio real image-model concepts should use controlled direct generation: pass exact structured flyer facts into the prompt and require a finished poster composition. Keep deterministic/Pillow smoke fallback separate; do not force production concepts through the old server overlay unless exporting legacy raw-background assets.
- Flyer Studio final packages must derive from the exact approved preview. When moving from raw-background compositing to direct poster generation, delete or ignore stale `*.raw.png` siblings; otherwise `APPROVE` can send a final that visually does not match the approved design.
- Flyer Studio approval replies must be case-insensitive and sender-block-safe. Real WhatsApp users reply `Approve`, `approve`, or `approve.`; do not route those into revision parsing just because the internal instruction says `APPROVE`.
- Flyer Studio active-project lookup must operate at the customer-account level, not only the exact inbound phone. The same onboarded business can have business WhatsApp, onboarded phone, LID, and authorized requester numbers; stale projects on one number must not swallow corrections meant for the latest project on another account number.
- Flyer Studio delivered files can still receive immediate customer corrections. Treat clear correction language after delivery as a revision path, and parse item swaps like `Swap Tatte Idly with Ghee Karam Idly` deterministically instead of asking for clarification.
- Flyer Studio free-trial quota is a product decision, not an implementation inference. Do not shrink it from 3 to 2 just because abuse was discussed unless the operator explicitly confirms the change for this rollout.
- Flyer Studio language selection should start with common customer languages for this market: English, Telugu, Hindi, Malayalam, Tamil, Kannada, and nearby Indian-language options where supported.
- Flyer Studio bare/vague action text such as `Create flyer` must never create a project. Treat it as intake/preflight and ask what the flyer should promote; otherwise repeated retries create empty `intake_started` projects with misleading acknowledgements.
- Flyer Studio briefs that say to extract/use items from an attached/sample/reference flyer are not render-ready unless the project actually has a reference media asset. Prompt for the attachment or typed details; do not let generic `items/prices` words satisfy the price-list readiness heuristic.
- Flyer Studio Guided Mode must preserve media attached to any guided answer and carry it into `create-flyer-project` as project-level reference media. Customers naturally attach the sample flyer while answering the first guided prompt, not only at final project creation.
- Flyer Studio language choice must be enforced in the render prompt, not merely stored in project fields. If Telugu is selected, the prompt must explicitly forbid all-English output and require Telugu-first headline/labels while preserving item names/prices.
- Flyer Studio processing acknowledgements need realistic timing expectations. Long image generation must say it usually takes 5-6 minutes and that the preview will be sent here, so customers do not think the system is broken and keep retrying.
- Before sending a Flyer Studio campaign after creative changes, verify the local `Flyer.png` hash against `/opt/shift-agent/state/flyer/marketing/Flyer.png` and refresh the VPS asset from the local file if needed. If WhatsApp appears to show stale media, send from a unique remote file path to avoid media-path cache confusion.
- Flyer Studio campaign CTAs and setup continuations must be deterministic for LID-only testers. Do not require `identify-sender` to resolve an E.164 phone before starting or continuing intake/onboarding; store pre-onboarding state by `chat_id` with `sender_phone=None` and block only owner/control-plane chats. A CTA fallthrough to the generic LLM is a production bug.
- Flyer Studio onboarding sessions can start as LID-only and later resolve to a phone. Session lookup must fall back to the `chat_id`-bound `sender_phone=None` session after a phone miss, and wrong-field replies must get Flyer Studio repair prompts instead of escaping to the generic model.
- Flyer Studio Text Mode completion is terminal for setup. After `Free trial active... Text Mode is ready`, the next flyer brief from that chat must route to project creation, including LID-only registered customers matched by `primary_chat_id`; never restart language/mode selection unless the customer explicitly starts setup again.
- Flyer Studio active/trial customers must be idempotent on campaign retries. If an already-registered LID-only customer taps `Start Free Trial` again or has a stale onboarding row, return the ready-to-create prompt and do not start intake/onboarding again.
- Do not call a Flyer Studio output an Instagram story unless the creative is actually generated and QA'd as a story-safe asset. If it is only a generic vertical image, label it honestly as a vertical/status image until platform-specific creative generation exists.
- Real WhatsApp CTA replies can arrive as the whole campaign card text plus the clicked label, not just the label or configured payload. Campaign routing must inspect the last visible nonempty line before falling back to onboarding/project classifiers.
- Flyer onboarding/intake intercept failures must not fall through to the generic Hermes LLM. Send a Flyer Studio-scoped recovery prompt and return `skip`, otherwise the generic agent can exhaust iterations and try unrelated self-improvement/tool edits.
- Admin dashboard campaign sends must surface backend errors in the UI and visually separate "dry run" from a real WhatsApp send. A silent 401/403 or a primary-looking dry-run button makes the operator think the campaign system is broken even when the backend send path is healthy.
- Media-backed event flyer edits such as "update this flyer, change name/date" are flyer work, not permanent brand-template replacement. Router heuristics must be business-category neutral and allow attached reference flyers to become project-level reference assets for any authorized requester.
- Flyer Studio exact edits to uploaded artwork are not the same as new flyer generation with a reference image. Requests like "remove the extra 08:00" or "change this date/name" must preserve the source flyer and should be queued for source-preserving/manual edit handling until a true image-edit pipeline exists; never send a recreated poster titled or framed as "Uploaded Flyer Template."
- Flyer Studio reference-scope authorization must be a continuation state, not a chat clarification loop. After the customer chooses authorized use, explains the relationship, and says to use saved account details, continue from the saved original request and uploaded media; do not ask them to resend details already present in state.
- Flyer Studio one-time paid orders must fail closed without a nonblank payment reference. Dashboard/CLI activation paths are money gates, so `--activate` without proof must never mark an order paid.
- Flyer Studio quick-flyer intake must treat unresolved LID-only sender identity as a scoped recovery prompt, not a subprocess call. Never pass `None` into guest-order CLI argv.
- Flyer Studio admin CSV campaign upload must send through the CSV endpoint after preview. Previewing uploaded targets and then sending the pasted-text box is operator-hostile and can silently send an empty or stale campaign.
- Flyer Studio JSON state updates that consume pending choices must hold one file lock across the full read-modify-write transaction and use `safe_io` atomic writers. A lock only around write, or a hand-rolled temp replace, still leaves TOCTOU windows.
- Sub-threshold PR review findings should still be preserved in a scoped follow-up backlog instead of being lost after merge. Keep them separate from unrelated active backlog edits so deployment commits stay traceable.
- Flyer Studio wrong-style output is rarely only a render prompt bug. Check parser fields, customer category capture, render policy, and QA blockers in that order; fixing style text alone can still ship poisoned project facts.
- Flyer Studio one-shot helper state must only be consumed when the full helper content is actually sent or otherwise intentionally used. Guided Mode questions are not starter prompts; do not burn starter prompt eligibility for them.
- Flyer Studio state mutations from cf-router must use the same locked script path as onboarding/intake/account CLIs. Avoid direct plugin writes to `customers.json` when adjacent production writers serialize through `manage-flyer-account` / `handle-flyer-*` locks.
- Fail-closed account-command handling should be scoped to the command family that needs it. Broad account words such as `help` and `status` from unknown senders should not block onboarding or generic routing just because sample-prompt preference commands need deterministic handling.
- Flyer Studio QA must include real WhatsApp transcript shape, especially text followed by media and short correction replies. A green local workbook/regression pass is not enough if the live flow can create a new incomplete project for each inbound and ask the customer to repeat themselves.
- Flyer Studio compound onboarding confirmation parsing must keep acceptance, starter suppression, and trailing-request routing in lockstep. If aliases like `ok create...` or `yes create...` complete onboarding, cf-router must extract and route the same trailing flyer request.
- 2026-05-19 Flyer Studio reference-scope answers must be remembered across follow-ups. If the customer chooses authorized use and gives relationship/details such as co-owner, store that context and continue; do not ask the same relationship/logo/details question repeatedly.
- Flyer Studio queued source-preserving edits must treat customer check-ins such as `any update`, `is it ready`, and `what's the status` as queue-status requests, not as new edit corrections. Never send those through revision parsing or ask for exact text again after the original change request is already queued.
- 2026-05-19 Flyer Studio E2E QA must visually inspect delivered pixels, not only text manifests. The manifest can self-report requested facts while the rendered poster still shows placeholders like `[price]` or ignores reference/menu content.
- 2026-05-19 Flyer Studio parser fields are a quality gate. Before blaming image generation, verify `event_or_business_name`, `venue_or_location`, schedule, item names, prices, and reference assets; poisoned fields make premium-looking flyers wrong.
- 2026-05-20 Flyer Studio customer copy helpers may serve multiple product contexts. Before selecting outcome-only wording from one incident, inspect every call site and choose copy that is truthful for all of them; source-edit-specific language does not belong in a generic manual-review/reference-fallback helper.
- 2026-05-20 Flyer Studio provider routing must not activate a paid/experimental provider from schema defaults or omitted caller args. Require an explicit config policy for OpenRouter source edits; absent policy and old callers should fail closed to manual review.
- 2026-05-20 Flyer Studio self-evaluation must treat QA reports as evidence only when they are tied to the current project asset/version/timestamp. A generic passed QA row, stale version, stale timestamp, or stray operator-review report can hide a current source-preservation failure.
- 2026-05-20 Flyer Studio operator briefs should summarize self-eval into actionable buckets and distinguish active customer-risk incidents from historical/audit findings. Avoid dumping raw incident rows into morning briefs.
- 2026-05-20 Flyer Studio report redaction must cover JSON, Markdown, and operator-brief paths, including key-based secret fields and bare/formatted phone numbers, not only obvious `Bearer` or `sk-` strings.
- 2026-05-21 Flyer Studio routing safety branches must sequence behind overlapping routing PRs. If another PR already owns fresh-intent bypass or customer deactivation, merge/deploy that first, rebase, then keep only non-duplicate observability/self-eval/operator-brief value so reviewers are not forced to untangle parallel fixes.
- 2026-05-21 Flyer Studio self-eval incidents with different meanings must not share the strictest evidence threshold. Routing anomalies such as a fresh flyer request found in revision history should emit from routing evidence alone; reserve missing-term thresholds for content-reflection incidents.
- 2026-05-21 Flyer Studio observability classifiers must delegate to the deployed routing helpers they monitor. A self-eval tripwire with a stricter local regex than `should_start_new_flyer_over_active()` creates false negatives for exactly the routing failures it is supposed to catch.
- 2026-05-21 Flyer Studio read-only routing previews must mirror live branch order, not just live predicates. Overlapping messages such as status text plus fresh flyer details should preview the same branch the hook reaches.
- 2026-05-21 Flyer Studio routing previews must mirror live exception gates as well as branch order. Open intake projects with required fields and similar repeated text should stay on the active project instead of previewing a new-project bypass.
- 2026-05-21 Flyer Studio routing preview route labels must describe the live outcome, not the nearest generic bucket. Repeated complete requests on an open intake project generate the active project; label that as active intake, not revision.
- 2026-05-22 Flyer Studio account/profile update commands must outrank active flyer project routing. Explicit phrases like `update business name to ...` are account-state changes, not queued flyer revisions; keep them in the account command family so stale/manual projects cannot swallow profile repairs.
# 2026-05-22 - Do not call offline training export "memory ingestion"

- Pattern: Hermes self-learning work can accidentally overclaim by naming a redacted JSONL export as "memory ingestion."
- Rule: If a change only writes an operator-reviewed artifact, name it `training_export` and keep live Hermes memory ingestion as a separate follow-up with an explicit receipt/proof path.
- Applies to: Flyer Hermes intent/self-learning PRs and any future agent self-evolution loop.
- 2026-05-22 Flyer Studio final approval must deliver the approved core WhatsApp final when it passes QA, even if optional Instagram/PDF derivatives fail OCR/visual QA. Optional derivative failures should be skipped and preserved for operator inspection, not block the customer from receiving the approved flyer.
- 2026-05-22 Flyer Studio generic pricing phrases such as `Price any item $9.99` are pricing policy, not menu item names. Apply the price to each explicitly included item and never lock `Price any item` / `Price any event` as a customer-visible item.
- 2026-05-22 Flyer Studio revision parsing must not fuzzy-replace pronouns like `it`, `this`, or `that` inside existing text. For schedule changes like `change it to Tuesday to Sunday`, append an explicit schedule instruction and preserve existing business names/notes.
- 2026-05-22 Flyer Studio negative language constraints outrank language-name mentions. `English only` / `do not use Telugu` must force English in extraction, render prompting, and visual QA even though the request contains the forbidden language name.
- 2026-05-23 Flyer Studio Hermes-as-brain boundary: let Hermes classify messy customer intent, but keep deterministic code responsible for hard contracts such as locked prices, approval/finalization state, status check priority, and customer-safe failure copy. Do not add broad custom "brain" heuristics when a narrow contract extraction or lifecycle guard is enough.
- 2026-05-23 Flyer Studio offer-price wording such as `all you can eat @ $25.99` is a required visible flyer fact. Preview/final QA must fail if the exact offer price is absent, even if the generated design contains plausible per-item prices.
- 2026-05-23 Flyer Studio category price revisions such as `Update prices of any biryani to $22.99` are policy updates, not item-name lookups. Append an explicit category-price instruction and regenerate; do not ask for exact text just because no existing item line matches the whole phrase.
- 2026-05-23 Flyer Studio status check routing must outrank manual-queue correction parsing. Phrases like `Where is the update flyer?` should return the current project/manual-review status, not ask the customer for exact text again.

- 2026-05-24 Flyer Studio explicit sample/example prompt requests are not flyer briefs. Phrases like `Can you give me sample prompt for evening snacks flyer` must route to sample ideas/starter prompts and must not create an incomplete project or ask what the flyer should promote. Keep preference commands like `don't show sample prompts` in account settings, but treat `give/show/send sample prompt/example/idea for ... flyer` as a customer-help intent.
- 2026-05-25 Flyer Studio residual issue reports should be converted into bounded fixes before handoff when the user asks to fix all issues. For source-edit/manual-queue incidents, distinguish live customer-visible stuck state, runtime config posture, recovery-classifier false positives, and delivery observability as separate levers.
- 2026-05-25 Flyer Studio source-edit integrity manifests must not run full poster copy-fit checks against operator/customer edit instructions. For source-preserving edits, verify artifact integrity and require visual/customer approval; classify deterministic fit failures as visual/layout QA, not provider timeout.
- 2026-05-25 Flyer Studio active brand assets can poison generation when a saved logo/template actually contains another business. If visual QA reports missing required `business_name` on a saved-logo request, retry once without saved brand assets before parking the customer in manual review.
- 2026-05-25 Flyer Studio successful autonomous regeneration from `manual_edit_required` must clear queued manual-review state. Otherwise the customer can receive a preview while operator/autorepair queues still report the project as stuck.
- 2026-05-25 Flyer Studio preview-change wording can contain `create a new flyer` while still being a revision to the active preview. If the message asks layout/emphasis changes such as making contact/address smaller or making services the main focus, append an actionable revision instruction and regenerate; do not ask for exact text just because no field value changed.
- 2026-05-25 Flyer Studio account-scope extraction must inspect nested business mentions, not only `flyer for <name>` at the front of a request. Phrases like `sales for Memorial Day on groceries for Triveni supermarket` should block under an MK Kitchen account, while `sales for Memorial Day on groceries` must remain a normal flyer brief.
- 2026-05-25 Flyer Studio registered-customer follow-ups can omit the word `flyer` while still being flyer details. Price/menu/promo replies such as `meal combo ... 49.99 ... Memorial Day weekend` should stay in Flyer Studio for active/trial accounts instead of falling through to generic Hermes chat.
- 2026-05-25 Flyer Studio final-delivery CTAs must be account-state-aware. Never send a `Start Free Trial` deep link to an already registered/free/trial customer after delivering their flyer; use `Create another flyer` and `Upgrade plan` actions, and route legacy trial-link clicks from registered customers to an account-aware recovery reply instead of starter/onboarding flows.
- 2026-05-25 Flyer Studio delivery CTA text and account command grammar must be changed together. If outbound copy says `UPGRADE PLAN`, that exact inbound text must be a deterministic account command that shows plans; otherwise clicks fall through to flyer intake and ask what the flyer should promote.
- 2026-05-25 Flyer recovery incidents must close on customer-visible success, not only on internal worker completion. A worker draft, bundle, or ack is not the repair; successful visible outcome rows such as corrected recovery replies or delivered flyer assets should resolve older matching incidents and emit `flyer_recovery_resolved`.
- 2026-05-25 Flyer recovery worker completion without customer-visible success evidence is not resolved. Mark it `operator_action_required`, emit a typed audit row, and surface it in self-eval as active customer risk so the repair engine fails forward instead of leaving customers in limbo.
- 2026-05-25 Flyer Studio billing CTAs must be role-aware. Do not show direct `CHANGE PLAN ...` commands to authorized requesters who can create flyers but cannot change billing; show plans plus the business-WhatsApp/account-owner path, and make attempted plan changes from non-admins return that path instead of a blunt denial.
- 2026-05-25 Flyer Studio account-admin checks must use registered WhatsApp identity, not only `sender_phone`. WhatsApp can deliver account commands with a LID or missing phone after the customer record was matched by chat id; the registered business WhatsApp number, onboarded owner number, and primary chat id must be allowed to maintain account settings, and natural commands like `Upgrade plan to Growth` must map to the same plan-change path as `CHANGE PLAN GROWTH`.
- 2026-05-25 Flyer Studio regulated account/billing language must fail closed before generic Hermes fallback. Phrases like `Upgrade to Growth`, `I want the 60 flyers/month plan`, or `Did my payment go through?` must either enter a deterministic account handler or receive a no-action clarification; generic chat must never improvise `processed`, `upgraded`, `changed`, or payment-success wording without a validated account/payment state transition.
- 2026-05-25 Flyer Studio semantic intent must normalize into an action registry, not directly mutate state. Hermes/classifiers may identify `I want the 60 flyers plan` or `change flyer request number`, but the registry and payment state machine remain the authority for admin, confirmation, and payment requirements.
- 2026-05-26 Flyer Studio customer SLA/status watchdogs must dedupe by resolved customer chat, not only by project row. Multiple stale projects for the same chat can create duplicate WhatsApp progress updates in the same minute unless every same-chat duplicate row is stamped/throttled when one customer update is sent.
