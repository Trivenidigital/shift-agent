Review the reconciliation merge HEAD (`b24ec07`) on branch `reconcile/flyer-box-to-main` vs `origin/main`. Read `git diff origin/main...HEAD`, read-only.

## Context
The main-vps box was running an UNMERGED stale branch `de44f17` (fix/flyer-customer-qa-cleanup, 5 flyer commits, ~43 commits behind) deployed directly — a deploy-drift. This merge reconciles de44f17 INTO current origin/main (which has Fix A–E firewall + CD observability + the #458 extractor-truth fix) so the box can return to ONE reviewed commit. de44f17 brings: a deterministic combo `_offers_from_source` parser, an "on the occasion of" campaign parser, advisory-planner gating (`requests_generated_item_suggestions` in facts/render/visual_qa), bare-trunk revision routing recognizers (cf-router actions/hooks), intake_fields, and scripts.

The full suite is GREEN (3423 passed / 0 failed) including the golden-scenario + cf-router routing suites. Four auto-merge semantic conflicts were hand-fixed:
1. `semantic_brief._fallback_brief`: the combo parser's offers now pass through `_offer_is_faithful` (the #458 gate) — so de44f17's combo parser can't lock a request-tail echo as a fact.
2. `schemas.CfRouterIntercepted.reason`: added `flyer_bare_brief_generation_dispatched` (emitted by de44f17's hooks.py).
3. `actions.is_flyer_revision_intent`: removed `update` keyword (origin/main golden-pinned: "any update?" is a STATUS check, not a revision).

## Review focus (flag BLOCKER/MAJOR/MINOR/NIT with file:line)
1. **Routing safety (highest priority):** the cf-router `actions.py` (+~276) and `hooks.py` (+~254) reconcile de44f17's stale routing with origin/main's evolved routing (regulated-intent, intake-bypass, P0-2). Are there OTHER stale-de44f17 routing regressions the tests might miss — precedence/ordering bugs, a recognizer that hijacks a request, a gate that silently drops, or origin/main routing logic that got reverted by the merge? Verify the bare-trunk revision recognizers compose correctly with origin/main's intercept ladder.
2. **Extractor reconciliation:** does de44f17's combo parser + the `_offer_is_faithful` gate + #458 compose correctly (no double-processing, no legit-combo over-drop, no echo under-drop)? Is the advisory-planner gating (inferred items stay required=False / not rendered unless asked) intact?
3. **No origin/main regressions:** did the merge accidentally revert any Fix A–E / observability / #458 / regulated-intent logic that origin/main had and de44f17 (stale) lacked? (The merge should be origin/main ∪ de44f17, never origin/main minus something.)
4. **Schema/audit + the 3 hand-fixes:** are they correct and complete?

This will deploy to the live box, replacing the drifted modules. If you judge the routing reconciliation too risky to deploy wholesale vs. a narrower path, say so. CLEAN if no findings.
